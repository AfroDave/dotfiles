ext x?html?, has firefox, X, flag f = firefox "$@"

mime ^text, label editor = "${EDITOR}" -- "$@"
mime ^text, label pager = "${PAGER}" -- "$@"
!mime ^text, label editor, ext xml|csv|tex|py|pl|rb|sh|php|c|cpp|clj|java|js|rs = "${EDITOR}" -- "$@"
!mime ^text, label pager, ext xml|csv|tex|py|pl|rb|sh|php|c|cpp|clj|java|js|rs = "${PAGER}" -- "$@"

mime ^video, has mpv, X, flag f = mpv -- "${@}"

mime ^image/svg*, has rsvg-view-3, X, flag f = rsvg-view-3 -- "${@}"
mime ^image, has sxiv, X, flag f = sxiv -- "${@}"
mime ^image, has display, X, flag f = display -- "${@}"

ext pdf, has zathura, X, flag f = zathura -- "${@}"

ext od[dfgpst]|docx?|sxc|xlsx?|xlt|xlw|gnm|gnumeric, has libreoffice, X, flag f = libreoffice "${@}"

ext 7z|ace|ar|arc|bz2?|cab|cpio|cpt|deb|dgc|dmg|gz, has als = als -- "${@}" | "${PAGER}"
ext iso|jar|msi|pkg|rar|shar|tar|tgz|xar|xpi|xz|zip, has als = als -- "${@}" | "${PAGER}"
ext 7z|ace|ar|arc|bz2?|cab|cpio|cpt|deb|dgc|dmg|gz, has aunpack = aunpack -- "${@}"
ext iso|jar|msi|pkg|rar|shar|tar|tgz|xar|xpi|xz|zip, has aunpack = aunpack -- "${@}"
